<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.playerlagbe">

    <!-- Internet permission (still needed for future features) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.playerlagbe">

        <!-- Main Activity (Default Launcher) - Now serves as Fragment Container -->
        <activity 
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication Activities (kept for authentication flow) -->
        <activity 
            android:name=".LauncherActivity"
            android:exported="false" />

        <activity 
            android:name=".AuthenticationActivity"
            android:exported="false" />

        <activity 
            android:name=".SignupActivity"
            android:exported="false" />

        <activity 
            android:name=".ForgotPasswordActivity"
            android:exported="false" />

        <!-- Admin Activities -->
        <activity 
            android:name=".ManageShopActivity"
            android:exported="false" />

        <activity 
            android:name=".ManageTeamActivity"
            android:exported="false" />

        <activity 
            android:name=".ManageOrdersActivity"
            android:exported="false" />

    </application>

</manifest>

